export default function newProjectFormReducer(state={errors: [], creatingProject: false}, action) {
  switch (action.type) {
    case "ADD_PROJECTS":
      break;
    default:
      return state
  }
}
